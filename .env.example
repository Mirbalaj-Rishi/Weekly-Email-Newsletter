# Copy to .env (gitignored) and fill in for local one-time manual deploys.
# These are config NAMES only — never put real values in a file that gets committed.

SENDER_EMAIL=
RECIPIENT_EMAILS=
RECIPIENTS_PARAM_NAME=/newsletter/recipients
GITHUB_REPO=
CDK_DEFAULT_ACCOUNT=
CDK_DEFAULT_REGION=us-east-1
